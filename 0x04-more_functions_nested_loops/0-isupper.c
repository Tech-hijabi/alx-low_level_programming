#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: the character being checked
 *
 * Return: 1 (success) or 0 (otherwise)
 */
int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
